<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <LinearLayout
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="6dp">
        <com.xw.repo.VectorCompatTextView
            app:drawableLeftCompat="@drawable/ic_sort"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:fontFamily="casual"
            android:padding="8dp"
            android:text="@string/show"
            android:textStyle="bold" />
        <org.angmarch.views.NiceSpinner
            android:id="@+id/nice_spinner"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:arrowTint="@color/view_background"
            app:backgroundSelector="@drawable/spinner_selector"
            app:textAllCaps="true"
            app:textTint="@android:color/holo_red_light" />
        </LinearLayout>
    <LinearLayout
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <com.xw.repo.VectorCompatTextView
            app:drawableLeftCompat="@drawable/ic_paixu_"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:fontFamily="casual"
            android:padding="8dp"
            android:text="@string/sort_by"
            android:textStyle="bold" />
        <org.angmarch.views.NiceSpinner
            android:id="@+id/nice_spinner2"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:arrowTint="@color/view_background"
            app:backgroundSelector="@drawable/spinner_selector"
            app:textAllCaps="true"
            app:textTint="@android:color/holo_red_light" />
    </LinearLayout>
    <ProgressBar
        android:id="@+id/loader"
        style="@style/Widget.AppCompat.ProgressBar.Horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="top"
        android:indeterminate="true" />
</LinearLayout>